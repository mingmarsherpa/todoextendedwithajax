@model TodoView.Pages.Admin.Users.IndexModel

<div class="modal-header border-0 pb-0">
    <div>
        <h5 class="modal-title">@Model.ModalTitle</h5>
        <p class="text-muted mb-0">@Model.ModalDescription</p>
    </div>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>

<div class="modal-body pt-3">
    <form method="post"
          action="@Model.FormAction"
          class="js-ajax-form"
          novalidate
          data-refresh-target="#user-list-container"
          data-success-action="edit"
          data-success-title="User updated"
          data-success-tone="success">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="alert alert-danger p-2 small"></div>
        <input asp-for="EditInput.Id" type="hidden" />

        <div class="row">
            <div class="col-md-6 mb-3">
                <label asp-for="EditInput.FirstName" class="form-label"></label>
                <input asp-for="EditInput.FirstName" class="form-control" />
                <span asp-validation-for="EditInput.FirstName" class="text-danger small"></span>
            </div>
            <div class="col-md-6 mb-3">
                <label asp-for="EditInput.LastName" class="form-label"></label>
                <input asp-for="EditInput.LastName" class="form-control" />
                <span asp-validation-for="EditInput.LastName" class="text-danger small"></span>
            </div>
        </div>

        <div class="mb-3">
            <label asp-for="EditInput.Email" class="form-label"></label>
            <input asp-for="EditInput.Email" class="form-control" />
            <span asp-validation-for="EditInput.Email" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="EditInput.Address" class="form-label"></label>
            <input asp-for="EditInput.Address" class="form-control" />
            <span asp-validation-for="EditInput.Address" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="EditInput.Role" class="form-label"></label>
            <select asp-for="EditInput.Role" class="form-select">
                @foreach (var role in Model.AvailableRoles)
                {
                    <option value="@role">@role</option>
                }
            </select>
            <span asp-validation-for="EditInput.Role" class="text-danger small"></span>
        </div>

        <div class="row">
            <div class="col-md-6 mb-3">
                <label asp-for="EditInput.NewPassword" class="form-label"></label>
                <input asp-for="EditInput.NewPassword" class="form-control" />
                <span asp-validation-for="EditInput.NewPassword" class="text-danger small"></span>
            </div>
            <div class="col-md-6 mb-3">
                <label asp-for="EditInput.ConfirmPassword" class="form-label"></label>
                <input asp-for="EditInput.ConfirmPassword" class="form-control" />
                <span asp-validation-for="EditInput.ConfirmPassword" class="text-danger small"></span>
            </div>
        </div>

        <div class="alert alert-light border mb-0">
            <strong>Security notes:</strong> Changing the email updates the username. Admins keep normal user access. Leave password fields blank if no reset is needed.
        </div>

        <div class="d-flex justify-content-end gap-2 mt-4">
            <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-primary">@Model.SubmitButtonText</button>
        </div>
    </form>
</div>
