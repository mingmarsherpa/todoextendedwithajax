@model TodoView.Pages.Admin.Users.IndexModel

<div class="modal-header border-0 pb-0">
    <div>
        <h5 class="modal-title">@Model.ModalTitle</h5>
        <p class="text-muted mb-0">@Model.ModalDescription</p>
    </div>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>

<div class="modal-body pt-3">
    <form method="post"
          action="@Model.FormAction"
          class="js-ajax-form"
          novalidate
          data-refresh-target="#user-list-container"
          data-success-action="create"
          data-success-title="User created"
          data-success-tone="success">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="alert alert-danger p-2 small"></div>

        <div class="row">
            <div class="col-md-6 mb-3">
                <label asp-for="CreateInput.FirstName" class="form-label"></label>
                <input asp-for="CreateInput.FirstName" class="form-control" />
                <span asp-validation-for="CreateInput.FirstName" class="text-danger small"></span>
            </div>
            <div class="col-md-6 mb-3">
                <label asp-for="CreateInput.LastName" class="form-label"></label>
                <input asp-for="CreateInput.LastName" class="form-control" />
                <span asp-validation-for="CreateInput.LastName" class="text-danger small"></span>
            </div>
        </div>

        <div class="mb-3">
            <label asp-for="CreateInput.Email" class="form-label"></label>
            <input asp-for="CreateInput.Email" class="form-control" />
            <span asp-validation-for="CreateInput.Email" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="CreateInput.Address" class="form-label"></label>
            <input asp-for="CreateInput.Address" class="form-control" />
            <span asp-validation-for="CreateInput.Address" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="CreateInput.Role" class="form-label"></label>
            <select asp-for="CreateInput.Role" class="form-select">
                @foreach (var role in Model.AvailableRoles)
                {
                    <option value="@role">@role</option>
                }
            </select>
            <span asp-validation-for="CreateInput.Role" class="text-danger small"></span>
        </div>

        <div class="row">
            <div class="col-md-6 mb-3">
                <label asp-for="CreateInput.Password" class="form-label"></label>
                <input asp-for="CreateInput.Password" class="form-control" />
                <span asp-validation-for="CreateInput.Password" class="text-danger small"></span>
            </div>
            <div class="col-md-6 mb-3">
                <label asp-for="CreateInput.ConfirmPassword" class="form-label"></label>
                <input asp-for="CreateInput.ConfirmPassword" class="form-control" />
                <span asp-validation-for="CreateInput.ConfirmPassword" class="text-danger small"></span>
            </div>
        </div>

        <div class="d-flex justify-content-end gap-2 mt-4">
            <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-primary">@Model.SubmitButtonText</button>
        </div>
    </form>
</div>
